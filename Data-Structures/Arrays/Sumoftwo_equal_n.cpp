//============================================================================
// Name        : Sumoftwo_equalton.cpp
// Author      : 
// Version     :
// Copyright   : Your copyright notice
// Description : Hello World in C++, Ansi-style
//============================================================================

#include <iostream>
#include <cstdlib>
#include <vector>
#include <map>
using namespace std;

int main() {
    //int n =100;
	string srt = "asdasjbkdsv"
    vector<char> b (srt.size());

    //int size = sizeof(a)/size(a[0]);
    map <int, int> m;
    for(int i=0;i<a.size();i++)
    {
        m[i] = 100-a[i];
    }
    for(int i=0;i<a.size();i++)
    {
    	int v = a[i];
        if(m.find(v)!=m.end())
        {
            cout<<a[i]<<","<<(100-a[i]);
        }
    }
    return 0;
}
